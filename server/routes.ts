// This file is replaced by NestJS controllers
import { Express } from "express";
import { Server } from "http";

export async function registerRoutes(app: Express, server: Server) {
  return server;
}
